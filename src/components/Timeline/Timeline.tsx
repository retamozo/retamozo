import React, { FunctionComponent, PropsWithChildren } from "react";
import { ProgressBar } from "../ProgressBar";

const Timeline: FunctionComponent<PropsWithChildren> = ({}) => {
  return (
    <div>
      <ProgressBar />
    </div>
  );
};

export default Timeline;
